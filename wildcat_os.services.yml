services:
  wildcat_os.flavor_manager:
    # Should implement \Drupal\wildcat_os\FlavorManagerInterface
    class: '\Drupal\wildcat_os\FlavorManager'
    arguments:
      - '@app.root'
      - '@site.path'
